# Aquest workflow ha estat substituït pel desplegament automàtic de Vercel.
# Vercel detecta els pushes a main/master i fa el build + deploy automàticament.
# La clau GROQ_API_KEY s'ha de configurar a: Vercel → Project → Settings → Environment Variables
