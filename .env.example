# Clau de l'API de Groq per al proxy local (npm run dev)
# Obté la teva clau a: https://console.groq.com/keys
# En producció (Vercel), configura-la a: Project → Settings → Environment Variables
GROQ_API_KEY=your_groq_api_key_here
